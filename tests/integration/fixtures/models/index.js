const Orders = require('./orders');
const Rates = require('./rates');
const Products = require('./products');
const Publics = require('./public');

module.exports = { Products, Rates, Orders, Publics };
